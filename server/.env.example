API_KEY=sk-your-deepseek-key-here
API_BASE_URL=https://api.deepseek.com/v1
API_MODEL=deepseek-chat
API_TEMPERATURE=0.1
API_MAX_TOKENS=256
HOST=0.0.0.0
PORT=8765
RELOAD=true
LOG_LEVEL=INFO
ORIGINS=*

# Admin panel (/admin) — certificate upload login (URL direct access only)
# Comma-separated SHA-256 fingerprints of allowed admin certificates.
# openssl x509 -in admin.pem -outform DER | openssl dgst -sha256
ADMIN_CERT_FINGERPRINTS=
ADMIN_SESSION_HOURS=8
